---
interface Props {
  title: string;
  id?: string;
}
const { title } = Astro.props;
---

<div class="mb-10 sm:mb-12" data-reveal>
  <h2 class="text-2xl font-bold sm:text-3xl">{title}</h2>
  <div class="mt-3 h-1 w-16 rounded-full bg-teal"></div>
</div>
